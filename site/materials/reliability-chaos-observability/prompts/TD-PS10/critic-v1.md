# TD-PS10 Critic Prompt v1.0.0

逐项拒绝：无 source_ref；无方法选择理由；Oracle 与生成器同源；没有 fault；把 UNKNOWN 补成事实；让 AI 放行；把 fixture 写成 live；跨页复制不适用规则。必须核对四个 Oracle：端到端 deadline 不小于子调用但总链有界；call amplification 不超过预算；过载拒绝不产生退款写操作；恢复窗口队列与错误率回到基线。
