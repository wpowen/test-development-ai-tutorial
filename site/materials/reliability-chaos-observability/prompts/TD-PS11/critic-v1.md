# TD-PS11 Critic Prompt v1.0.0

逐项拒绝：无 source_ref；无方法选择理由；Oracle 与生成器同源；没有 fault；把 UNKNOWN 补成事实；让 AI 放行；把 fixture 写成 live；跨页复制不适用规则。必须核对四个 Oracle：task trace 覆盖 gateway retrieval model tool 和 terminal；敏感输入不进入默认 telemetry；fault start/end 与异常窗口可关联；停止回滚后同一切片恢复且无残留。
