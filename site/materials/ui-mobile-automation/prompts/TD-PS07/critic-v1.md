# TD-PS07 Critic Prompt v1.0.0

逐项拒绝：无 source_ref；无方法选择理由；Oracle 与生成器同源；没有 fault；把 UNKNOWN 补成事实；让 AI 放行；把 fixture 写成 live；跨页复制不适用规则。必须核对四个 Oracle：稳定 identifier 不依赖本地化文字；权限拒绝仍可完成安全替代路径；同一预约版本只应用一次改期；清理后通知日历 keychain 与后端状态回到基线。
