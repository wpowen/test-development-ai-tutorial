# AI foundations and evaluation fixture

This public learner bundle runs without credentials. It checks deterministic contracts for eleven pages and never calls a model. Run any manifest's baseline, fault and repair steps; expected exits are 0/1/0. `PASS` in reports means fixture checks only.
