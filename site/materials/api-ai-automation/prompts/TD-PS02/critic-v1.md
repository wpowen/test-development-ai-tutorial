# TD-PS02 Critic Prompt v1.0.0

逐项拒绝：无 source_ref；无方法选择理由；Oracle 与生成器同源；没有 fault；把 UNKNOWN 补成事实；让 AI 放行；把 fixture 写成 live；跨页复制不适用规则。必须核对四个 Oracle：amount 必须大于零且币种受商户支持；客户只能操作自己的支付意图；过期意图不能确认且状态不变；删除 required 或放宽金额必须杀死 mutation。
