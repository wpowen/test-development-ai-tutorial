openapi: 3.1.0
info: {title: Order cancellation fixture, version: v7}
paths:
  /orders/{order_id}/cancel:
    post:
      responses:
        '409': {description: shipped order cannot be cancelled}
        '403': {description: non-owner}
