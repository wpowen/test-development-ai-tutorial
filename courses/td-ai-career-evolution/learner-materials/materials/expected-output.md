# Expected output

一个合格输出至少包含：

- 任务、人口、分母、切片和错误成本；
- 独立 Oracle、指标定义、不确定性、owner 和失败动作；
- 输入/Schema/Eval/Mutation 的版本引用；
- `UNKNOWN`、`NOT_RUN`、`BLOCKED` 边界；
- 可复制的工件路径和下一次 reviewer 复评动作。

这只是结构和证据合同的 fixture 期望，不代表真实业务阈值、模型质量、职业等级或就业结果。
