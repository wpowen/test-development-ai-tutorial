# 快速开始

进入 lab 后依次运行 verify-packages、suite baseline、suite fault、suite repair。预期 0/0/1/0；fault 非零不能吞。打开 reports 对照 checks、failed_checks、prompt_manifest_hash、evidence_scope 和 model_evidence。当前仅为 stdlib 合成 fixture。

