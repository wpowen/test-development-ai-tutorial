# 合成输入说明

`learner-materials/fixtures/` 中两份 OAS 3.1.0 YAML 和 checkout 事件 JSON 都是合成夹具，不代表企业 API。它们定义订单取消、支付意图、异步任务、`text/event-stream`、重复投递与唯一终态；业务 Oracle 额外定义 SHIPPED 不可取消、非 owner 拒绝和重复请求单次副作用。未使用个人、真实支付或生产数据。
