apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: ai-gateway-pod-failure
  namespace: chaos-mesh
spec:
  action: pod-failure
  mode: one
  duration: 30s
  selector:
    namespaces: [demo]
    labelSelectors:
      app: ai-gateway
