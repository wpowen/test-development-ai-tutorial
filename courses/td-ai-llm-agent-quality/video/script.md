# Lesson script

“如果模型和检索器同时变了，你还能说回归来自模型吗？”先暂停，让学员圈出实验中唯一允许改变的变量。运行 TD-T13 baseline，确认五项锁、三次重复和无 blocker；注入 fault，退出码 1，报告指出 `SINGLE-VARIABLE`；修复后退出码 0。再提醒：同样的证据纪律适用于 Judge、工具权限、Browser Agent、healer 和 Workflow。此处没有真实模型调用，不能把 fixture 绿灯说成 live 或 practitioner 结论。
