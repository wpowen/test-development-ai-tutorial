# 期望结构

至少包含 `responsibility_statement`、九站 `lifecycle`、`document_reading`、`method_and_oracle`、带 owner/version/source/acceptance/consumer 的 `artifacts`、`release_boundary`、`ai_migration`、`learning_route` 与 `unknowns`。需求与技术冲突、Oracle 不独立或发布责任人缺失时，顶层 `status` 必须是 `BLOCKED`。这份结构只说明契约，不代表任何模型已经运行或从业者已批准。
